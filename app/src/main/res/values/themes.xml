<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/Shadows</item>
        <item name="colorPrimaryVariant">@color/Shadows</item>
        <item name="colorOnPrimary">@color/Shadows</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TestTask" parent="Base.Theme.TestTask" />
</resources>